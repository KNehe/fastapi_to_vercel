[project]
name = "fastapi-to-vercel"
version = "0.1.0"
description = "A JSON-backed FastAPI tutorial project for deployment to Vercel"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.135.3",
]
